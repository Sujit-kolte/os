// 1. Create a single field ascending index on 'name'
db.users.createIndex({ name: 1 });

// 2. Create a single field descending index on 'age'
db.users.createIndex({ age: -1 });

// 3. Create a compound index on 'name' ascending and 'age' descending
db.users.createIndex({ name: 1, age: -1 });

// 4. Create a unique index on 'email'
db.users.createIndex({ email: 1 }, { unique: true });

// 5. Create a TTL index on 'createdAt' field to expire documents after 1 hour
db.sessions.createIndex({ createdAt: 1 }, { expireAfterSeconds: 3600 });

// 6. Create a text index for text search on 'description' field
db.articles.createIndex({ description: "text" });

// 7. Create a hashed index for sharding on '_id'
db.users.createIndex({ _id: "hashed" });

// 8. List all indexes on 'users' collection
db.users.getIndexes();

// 9. Drop a specific index by name
db.users.dropIndex("name_1");

// 10. Drop all indexes except the default _id index
db.users.dropIndexes();

// 11. Rebuild all indexes on a collection
db.users.reIndex();

// 12. Check if a query uses index (execution stats)
db.users.find({ name: "John" }).explain("executionStats");

